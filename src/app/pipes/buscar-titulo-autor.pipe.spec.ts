import { BuscarTituloAutorPipe } from './buscar-titulo-autor.pipe';

describe('BuscarTituloAutorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarTituloAutorPipe();
    expect(pipe).toBeTruthy();
  });
});
